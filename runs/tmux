#!/usr/bin/env bash
set -euo pipefail

sudo pacman -S --noconfirm --needed tmux

# Install TPM (idempotent)
if [ ! -d "$HOME/.tmux/plugins/tpm" ]; then
	git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
else
	echo "[tmux] TPM already installed"
fi
