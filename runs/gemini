#!/usr/bin/env bash
set -euo pipefail

# Ensure node and npm are available
if ! command -v npm &>/dev/null; then
	echo "[gemini] npm not found — run the 'node' installer first"
	exit 1
fi

sudo npm install -g @google/gemini-cli
