# Global Claude Rules

## Git Commits

When performing git commits or git pushes, do NOT add the `Co-Authored-By: Claude` line to commit messages. Keep commit messages clean without Claude attribution.
